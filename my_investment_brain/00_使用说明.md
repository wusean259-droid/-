# 自定义知识库 — 怎么用

**知识库就是这个文件夹。** 里面的 `.md` / `.txt` 都是纯文本文件。

## 你可以这样加内容

1. **在本文件夹新建 .md**，用 Cursor / 记事本写或粘贴文字 → 网页点「重建索引」
2. **网页「✍️ 网页里直接写」** — 不用自己建文件
3. **网页「📤 上传已有文件」** — 选电脑里现成的 .md / .txt

`.md` 不是 Word，就是带标题格式的文本；**复制粘贴文字进去完全正确。**

Word / PDF 需先另存为 `.txt` 或 `.md` 再放入本文件夹。
